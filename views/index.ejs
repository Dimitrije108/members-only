<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Mini Message Board</title>
		<link rel="stylesheet" href="/styles.css">
	</head>
	<body>
		<% if (locals.user) { %>
			<h1>Wlcm trvlr <%= `${user.first_name} ${user.last_name}` %></h1>
			<a href="/auth/log-out">Log Out</a>
		<% } else { %>
			<h1>Hello There!</h1>
			<p>Please log-in or register to view posts.</p>
			<a href="/auth/log-in">Log In</a>
		<% } %>
	</body>
</html>